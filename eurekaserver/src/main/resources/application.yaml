spring:
  application:
    name: eurekaserver
  security:
    basic:
      enabled: true
    user:
      name: eureka-user-ms
      password: eureka-pass-ms

server:
  port: 8761

eureka:
  client:
    register-with-eureka: false
    fetch-registry: false
